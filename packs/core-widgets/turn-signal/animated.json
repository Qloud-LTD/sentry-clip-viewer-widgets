{
  "$schema": "widget-v1.json",
  "id": "turnSignal.turnSignalAnimated",
  "version": "1.0.0",
  "category": "turnSignal",
  "name": "Turn Signal Animated",
  "description": "Blinking arrow indicators (uses opacity binding for animation)",
  "author": "Built-in",

  "canvas": {
    "aspectRatio": 2.5,
    "minWidth": 0.08,
    "minHeight": 0.04
  },

  "bindings": {
    "leftColor": {
      "conditions": [
        { "when": "{{blinker.left}}", "value": "#00FF00" }
      ],
      "default": "rgba(255, 255, 255, 0.2)"
    },
    "leftOpacity": {
      "conditions": [
        { "when": "{{blinker.left}}", "value": "{{animation.blink}}" }
      ],
      "default": 0.3
    },
    "rightColor": {
      "conditions": [
        { "when": "{{blinker.right}}", "value": "#00FF00" }
      ],
      "default": "rgba(255, 255, 255, 0.2)"
    },
    "rightOpacity": {
      "conditions": [
        { "when": "{{blinker.right}}", "value": "{{animation.blink}}" }
      ],
      "default": 0.3
    }
  },

  "animations": {
    "blink": {
      "type": "oscillate",
      "from": 0.3,
      "to": 1.0,
      "duration": 0.5,
      "easing": "easeInOut"
    }
  },

  "elements": [
    {
      "type": "group",
      "layout": "hstack",
      "spacing": "15%w",
      "alignment": "center",
      "children": [
        {
          "type": "icon",
          "name": "arrowtriangle.left.fill",
          "size": "60%h",
          "color": "{{leftColor}}",
          "opacity": "{{leftOpacity}}"
        },
        {
          "type": "icon",
          "name": "arrowtriangle.right.fill",
          "size": "60%h",
          "color": "{{rightColor}}",
          "opacity": "{{rightOpacity}}"
        }
      ]
    }
  ]
}
